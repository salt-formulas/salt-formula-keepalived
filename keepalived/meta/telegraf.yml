agent:
  input:
    procstat:
      process:
        keepalived:
          exe: keepalived
    ipcheck:
      template: keepalived/files/telegraf.conf
      interface_name_regexp_exclude: '^docker.*'
